{
  "name": "strawpoll-clone",
  "version": "1.0.0",
  "description": "StrawPoll clone with React + Express + MongoDB",
  "scripts": {
    "start": "node backend/server.js",
    "render-build": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}